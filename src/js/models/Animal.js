import GameBaseObject from "./GameBaseObject";

export default class Animal extends GameBaseObject {
  constructor(name) {
    super(name);
  }

  update(ctx, imageSheet) {
    super.update(ctx, imageSheet);
  }
}
